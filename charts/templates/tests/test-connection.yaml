apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zpk.fullname" . }}-test-connection"
  labels:
    {{- include "zpk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zpk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
