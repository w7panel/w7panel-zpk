apiVersion: v1
kind: Service
metadata:
  name: {{ include "zpk.fullname" . }}
  labels:
    {{- include "zpk.labels" . | nindent 4 }}
    allow-proxy: "true"
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zpk.selectorLabels" . | nindent 4 }}
